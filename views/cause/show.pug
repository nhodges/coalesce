extends ../layouts/base.pug

block append vars
  - pageClasses.push('ct__causes')

block body-content
  header
    .shadow
  .ui.container.ct__userprofile__content
    pre= JSON.stringify(cause, 1, 2)

    pre= JSON.stringify(organizations, 1, 2)
